import { Routes } from '@angular/router';
import { CalendarPageComponent } from './calendar-page/calendar-page.component';

export const CALENDAR_ROUTES: Routes = [{ path: '', component: CalendarPageComponent }];
