/**
 * `MonthGridComponent` now lives in `shared/ui/month-grid` since the new
 * reusable `app-date-picker` (DEMOPROJEK-42) also renders it. Re-exported here
 * so this path keeps working.
 */
export { MonthGridComponent } from '../../../shared/ui/month-grid/month-grid.component';
